# Test file for error cases and edge cases

# Multiple values on one line (should create error node)
MULTIPLE=value1 value2 value3

# Missing equals sign (should create error)
MISSING_EQUALS

# Missing value (valid - creates empty value)
EMPTY_VALUE=

# Malformed interpolation
BAD_INTERPOLATION="${UNCLOSED
NESTED_INTERPOLATION="${FOO_${BAR}}"

# Invalid boolean (parsed as raw value, not bool)
BOOL_TYPO=ture
BOOL_CAPS=TRUE

# Trailing content after primitives
BOOL_WITH_EXTRA=true extra content
NUMBER_WITH_EXTRA=123 more stuff

# Special characters in keys (if supported)
KEY-WITH-HYPHENS=value
@NAMESPACE:KEY=value
//URL/STYLE/:KEY=value

# Very long values
LONG_VALUE=This is a very long value that might cause issues in some parsers but should be handled correctly by a well-designed parser that doesn't have arbitrary length limits

# Unicode in keys and values
EMOJI_KEY_🔑=value
UNICODE_VALUE=Hello 世界 مرحبا שלום

# Quotes mismatch
MISMATCHED="double to single'
UNCLOSED_DOUBLE="no closing quote
UNCLOSED_SINGLE='no closing quote

# Comments in unusual places
KEY=value # inline comment
# KEY=value (commented out variable)
KEY= # empty with comment

# Invalid syntax attempts
KEY==double_equals
=NO_KEY
KEY=VALUE=ANOTHER
KEY VALUE=no_separator
KEY:VALUE=wrong_separator